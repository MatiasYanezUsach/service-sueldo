spring:
  application:
    name: microservice-sueldo
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/sueldo
    username: root
    password: Chile&2022
  jpa:
    properties:
      hibernate:
        '[globally_quoted_identifiers]': true
        jdbc:
          lob:
            '[non_contextual_creation]': true
    hibernate:
      use-new-id-generator-mappings: false
      ddl-auto: create-drop
management:
  endpoints:
    web:
      exposure:
        include: '*'